# schema10 evidence integration: implementation status

Updated: 2026-09-06. This is an engineering handoff, not an experimental result.
Historical schema/config/result files retain their original interpretation.

## Changes implemented locally

| Area | Implementation | Evidence boundary |
| --- | --- | --- |
| Selection budget | Explicit `end_to_end_aware` policy; old `legacy_fixed_fraction` remains an ablation | No new 5% rejection; time is still measured and FinalCommit remains 0.8 |
| Shared selector | `Schema10CheckpointSelector` and `ProductionSelectionSession` implement the same decisions for online observations and replay | d1/d2 and legacy checkpoint decisions share code; the FAST tensor bridge does not pretend to implement legacy scheduling |
| Live K comparison | `LiveSelectionBridge` reads the paused request's pre-RoPE K and independent Source SelectionState | It never reads offline `fixture.current_layers` as current-request evidence, or loads full KV to compare candidates |
| Resources | One-shot comparison if its bounded workspace fits; otherwise microbatch; physical transfer still needs lease/HBM authorization | Removing a time gate does not remove resource admission |
| Final admission | Executor invokes the real joint subset planner, includes all segments and actual elapsed arrival-to-boundary time, preserves rejected Source IDs | Final future remains a profile estimate; this is not a promise that measured TTFT can never overrun |
| Failure handling | Full-KV transfer cleanup is registered on exception; serving harness retains failed requests | An all-abstain dense outcome is not a missing-FinalCommit error |
| Gate1 A/B | Both actual dispatch arms run from the same quiescent pool snapshot, differing only in Gate1 mode | A dense-vs-forced-reuse measurement cannot serve as this ablation |
| Coverage | Each K resets the pool independently at the same global byte budget; materialization happens after the current request | Future-Variant visibility, invalid selected/committed chains and changed budgets are rejected |
| Source Oracle | Sequential real dense and per-Source actions, fixed boundary and repair, actual QA-constrained first-token cost | Mechanism Oracle and serving TTFT are labeled separately; low residual is not an answer-quality Oracle |
| Arrival benchmark | Scheduled arrivals precede worker queuing; TTFT includes waiting; failures are retained | Backends must declare actual integrated concurrency; copy-interference kernels do not qualify |
| Profile freeze | New evidence contract rejects proxy-only aggregates and binds selection budget policy | No Profile, GPU qualification or paper result is generated by local tests |

## Time policy

The new policy does **not** require selection time to be below 5% of dense TTFT.
It does not require an incomplete incremental plan to already meet the final
0.8 target. Otherwise, early unresolved segments could be rejected before
later selection exposes a useful joint plan.

An optional early futility conclusion is legal only from a complete-scope lower
bound over feasible joint continuations. A current winner's predicted upper
cost is not such a bound. Actual probe/metadata/compare intervals are unioned,
not added twice when they overlap. These active-time measurements are not a
claim about causal slowdown relative to a counterfactual run.

The final request test remains:

`actual arrival-to-boundary elapsed + profiled complete joint future <= 0.8 * matched dense TTFT`

The dense reference must have the same request, Prefix Cache hit, serving load
and first-token endpoint. Quality, exact content, Source origin, leases and
memory feasibility remain independent constraints.

New local configurations:

```powershell
$env:PYTHONPATH="$PWD/src"
python -m probekv.cli --config configs/local_system_v8_schema10_gate1_barrier_end_to_end.json
python -m probekv.cli --config configs/local_system_v8_schema10_gate1_counterfactual_end_to_end.json
```

These are simulations, not a production launch command. Their deliberately
unfrozen thresholds/repair settings must not be interpreted as GPU certification.
Existing configurations without `selection_budget_policy` retain fixed 5%.
Profile freeze must use an explicitly matching `--selection-budget-policy`;
the policy is also bound into the immutable measurement and bundle hashes.

## Items still required, in order

1. Bind the complete Source Pool lookup/materialization/LRU lifecycle and
   measured joint RuntimeCostProfile provider to the experiment backend.
   Current experiment harnesses enforce the backend contract but are not a
   substitute for that deployment adapter. A fixture's prebuilt historical
   Sources are not an operational dynamic-pool experiment.
2. Validate FAST live selection and preserved legacy scheduling separately on
   the patched engine. The new FAST bridge deliberately refuses deep runtime
   checkpoints; a passing CPU selector replay is not a legacy GPU certificate.
3. Finish fit/validation-separated production event aggregation. Collect actual
   ready/load/admission timing, task quality and per-K causal runs before
   freezing Profiles. Keep the deep residual-score diagnostic distinct from
   the measured quality/cost Oracle.
4. Integrate the serving backend with true concurrent request state. The current
   fixture executor owns shared mutable KV/sampling state and must not be made
   concurrent merely by putting it behind a thread pool. It should advertise
   capacity one until isolation/scheduling is implemented and tested.
5. Only then freeze a new SHA and run new GPU correctness/qualification outputs,
   followed by the preregistered matched-quality baseline and H1-H5 experiments.
   Neither past diagnosis nor this local test run proves Q1-level benefit.

Until these items are evidenced, retain:

```text
schema10_local_implementation_complete = false
gpu_rental_ready_for_schema10_profile_freeze = false
gpu_runtime_qualified = false
h1_h2_execution_allowed = false
paper_evidence = false
locked_test_accessed = false
```

## Validation

Use Python >=3.10. This Windows workspace uses the ignored `.venv-validation`
Python 3.12 environment for CPU tests; no server environment was changed.

```powershell
$env:PYTHONPATH="$PWD/src"
python -m compileall -q src scripts tests
python -m unittest discover -s tests -v
python scripts/validate_contract.py
git diff --check
```

The tests cover replay policy/hash binding, Source16 selection from live K,
budget-mode separation, complete-inventory FinalCommit partial rejection,
stale snapshots/non-finite costs, real A/B arm invocation, independent K pools,
QA-constrained Oracle decisions, queue timing and retained serving failures.
