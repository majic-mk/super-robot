diff --git a/vllm_blend/vllm/attention/backends/xformers.py b/vllm_blend/vllm/attention/backends/xformers.py
index 6a85dbe..c926155 100644
--- a/vllm_blend/vllm/attention/backends/xformers.py
+++ b/vllm_blend/vllm/attention/backends/xformers.py
@@ -203,6 +203,7 @@ class XFormersImpl(AttentionImpl):
         
         repair_regions = cache_fuse_metadata.get("repair_regions")
         resumable_mode = bool(cache_fuse_metadata.get("probekv_resumable"))
+        dense_equivalent_full_repair = False
         if status in [1] and resumable_mode:
             active_positions = cache_fuse_metadata["active_positions"].to(
                 device=query.device, dtype=torch.long
@@ -221,29 +222,52 @@ class XFormersImpl(AttentionImpl):
             key_old[target_positions] = key[local_indices]
             value_old[target_positions] = value[local_indices]
             query = query[local_indices]
-            key = key_old
-            value = value_old
             cache_fuse_metadata["local_imp_indices"] = local_indices
             cache_fuse_metadata["imp_indices"] = target_positions
             if (
                 torch.equal(active_positions, target_positions)
                 and not cache_fuse_metadata.get("exact_prefix_tokens", 0)
             ):
-                # r=1 keeps every query row. Use the identical dense causal
-                # bias object so full repair is numerically dense-equivalent.
-                cache_fuse_metadata["attn_bias"] = (
-                    BlockDiagonalCausalMask.from_seqlens(
-                        attn_metadata.prefill_metadata.prompt_lens
-                    )
-                )
+                # r=1 has no historical attention rows left. Keep Source
+                # writeback/audit semantics, but execute the exact native
+                # dense attention path instead of the partial-reuse kernel.
+                dense_equivalent_full_repair = True
             else:
+                key = key_old
+                value = value_old
                 cache_fuse_metadata["attn_bias"] = _make_partial_bias_gqa(
                     cache_fuse_metadata,
                     query.device,
                     self.num_kv_heads,
                     self.num_queries_per_kv,
                 )
-            attn_metadata.prefill_metadata.attn_bias = None
+                attn_metadata.prefill_metadata.attn_bias = None
+
+        if status in [2] and resumable_mode:
+            # Every still-active query row is recomputed at every later layer.
+            # Preserve historical KV only for inactive reuse rows; otherwise
+            # r=1 would silently reinstall Source KV after its boundary layer.
+            active_positions = cache_fuse_metadata["active_positions"].to(
+                device=query.device, dtype=torch.long
+            )
+            if active_positions.numel() != query.shape[0]:
+                raise ValueError("active positions do not match query rows")
+            if not torch.all(active_positions[1:] > active_positions[:-1]):
+                raise ValueError("active positions must be strictly increasing")
+            key_old[active_positions] = key
+            value_old[active_positions] = value
+            full_prompt_positions = torch.arange(
+                int(cache_fuse_metadata["org_seq_len"]),
+                device=query.device,
+                dtype=torch.long,
+            )
+            dense_equivalent_full_repair = (
+                not cache_fuse_metadata.get("exact_prefix_tokens", 0)
+                and torch.equal(active_positions, full_prompt_positions)
+            )
+            if not dense_equivalent_full_repair:
+                key = key_old
+                value = value_old
 
         if status in [1] and repair_regions is not None and not resumable_mode:
             last_len = int(cache_fuse_metadata["suffix_len"])
@@ -404,6 +428,7 @@ class XFormersImpl(AttentionImpl):
         if (
             status == 2
             and resumable_mode
+            and not dense_equivalent_full_repair
             and cache_fuse_metadata.get("attn_bias") is None
         ):
             cache_fuse_metadata["attn_bias"] = _make_partial_bias_gqa(
@@ -423,7 +448,7 @@ class XFormersImpl(AttentionImpl):
             #value = value_old
             
         
-        if status in [2]:
+        if status in [2] and not resumable_mode:
             imp_indices = cache_fuse_metadata["imp_indices"]
             key_old[imp_indices] = key 
             value_old[imp_indices] = value
@@ -459,7 +484,8 @@ class XFormersImpl(AttentionImpl):
         
         
         # TODO(Jiayi): need to change num prefill tokens accordingly
-        if status in [1,2]:
+        attention_status = 0 if dense_equivalent_full_repair else status
+        if attention_status in [1,2]:
             num_prefill_tokens = attn_metadata.num_prefill_tokens
             num_decode_tokens = attn_metadata.num_decode_tokens
             output = torch.empty_like(query)
@@ -495,7 +521,8 @@ class XFormersImpl(AttentionImpl):
                 # prefix.
                 
                 out = self._run_memory_efficient_xformers_forward(
-                    query, key, value, prefill_meta, status, cache_fuse_metadata)
+                    query, key, value, prefill_meta, attention_status,
+                    cache_fuse_metadata)
                 
                 #assert out.shape == output[:num_prefill_tokens].shape
                 #output[:num_prefill_tokens] = out
diff --git a/vllm_blend/vllm/model_executor/models/llama.py b/vllm_blend/vllm/model_executor/models/llama.py
index 81be9d2..ec2d7a3 100644
--- a/vllm_blend/vllm/model_executor/models/llama.py
+++ b/vllm_blend/vllm/model_executor/models/llama.py
@@ -362,6 +362,7 @@ class LlamaModel(nn.Module):
             "model_signature": str(model_signature),
             "attn_bias": None,
             "fake_q": None,
+            "dense_full_repair_endpoint": False,
             "reuse_active": bool(
                 self.cache_fuse_metadata.get("exact_prefix_tokens", 0)
             ),
@@ -388,8 +389,21 @@ class LlamaModel(nn.Module):
         shrinking = not torch.equal(active, target)
         transition = bool(reuse_commit)
         prefix_active = bool(metadata.get("exact_prefix_tokens", 0))
-        status = 1 if transition else (
-            2 if metadata["reuse_active"] or prefix_active else 0
+        full_prompt_positions = torch.arange(
+            metadata["org_seq_len"], dtype=torch.long,
+            device=hidden_states.device)
+        dense_full_repair = (
+            transition
+            and not prefix_active
+            and not metadata["reuse_active"]
+            and torch.equal(active, target)
+            and torch.equal(active, full_prompt_positions)
+        )
+        metadata["dense_full_repair_endpoint"] = dense_full_repair
+        status = 0 if dense_full_repair else (
+            1 if transition else (
+                2 if metadata["reuse_active"] or prefix_active else 0
+            )
         )
         start = torch.cuda.Event(enable_timing=True)
         end = torch.cuda.Event(enable_timing=True)
@@ -400,7 +414,7 @@ class LlamaModel(nn.Module):
             self.old_kvs[int(layer) - 1])
         end.record()
         end.synchronize()
-        if transition:
+        if transition and not dense_full_repair:
             metadata["reuse_active"] = True
         return {
             "hidden_states": hidden_states,
@@ -420,6 +434,7 @@ class LlamaModel(nn.Module):
         self.cache_fuse_metadata["probekv_resumable"] = False
         self.cache_fuse_metadata["check"] = False
         self.cache_fuse_metadata["reuse_active"] = False
+        self.cache_fuse_metadata["dense_full_repair_endpoint"] = False
         self.cache_fuse_metadata["exact_prefix_tokens"] = 0
         return hidden_states
 
diff --git a/vllm_blend/vllm/model_executor/models/llama.py b/vllm_blend/vllm/model_executor/models/llama.py
index ec2d7a3..b5f23cc 100644
--- a/vllm_blend/vllm/model_executor/models/llama.py
+++ b/vllm_blend/vllm/model_executor/models/llama.py
@@ -424,6 +424,17 @@ class LlamaModel(nn.Module):
             "host_ms": 0.0,
             "union_mask_digest": str(metadata.get(
                 "current_union_mask_digest", "")),
+            "runtime_debug": {
+                "status": int(status),
+                "transition": transition,
+                "prefix_active": prefix_active,
+                "reuse_active_after": bool(metadata["reuse_active"]),
+                "active_equals_target": bool(torch.equal(active, target)),
+                "active_equals_full_prompt": bool(
+                    torch.equal(active, full_prompt_positions)
+                ),
+                "dense_full_repair": bool(dense_full_repair),
+            },
         }
 
     def probekv_finish_prefill(self, hidden_states, residual,
diff --git a/vllm_blend/vllm/model_executor/models/qwen2.py b/vllm_blend/vllm/model_executor/models/qwen2.py
index 23cbc10..a03bb35 100644
--- a/vllm_blend/vllm/model_executor/models/qwen2.py
+++ b/vllm_blend/vllm/model_executor/models/qwen2.py
@@ -372,6 +372,17 @@ class Qwen2Model(nn.Module):
             "gpu_ms": float(start.elapsed_time(end)), "host_ms": 0.0,
             "union_mask_digest": str(metadata.get(
                 "current_union_mask_digest", "")),
+            "runtime_debug": {
+                "status": int(status),
+                "transition": transition,
+                "prefix_active": prefix_active,
+                "reuse_active_after": bool(metadata["reuse_active"]),
+                "active_equals_target": bool(torch.equal(active, target)),
+                "active_equals_full_prompt": bool(
+                    torch.equal(active, full_prompt_positions)
+                ),
+                "dense_full_repair": bool(dense_full_repair),
+            },
         }
 
     def probekv_finish_prefill(self, hidden_states, residual,
diff --git a/vllm_blend/vllm/model_executor/models/qwen2.py b/vllm_blend/vllm/model_executor/models/qwen2.py
index 790f27a..23cbc10 100644
--- a/vllm_blend/vllm/model_executor/models/qwen2.py
+++ b/vllm_blend/vllm/model_executor/models/qwen2.py
@@ -312,7 +312,8 @@ class Qwen2Model(nn.Module):
             "target_active_positions": absolute,
             "imp_indices": absolute, "local_imp_indices": None,
             "model_signature": str(model_signature), "attn_bias": None,
-            "fake_q": None, "reuse_active": bool(
+            "fake_q": None, "dense_full_repair_endpoint": False,
+            "reuse_active": bool(
                 self.cache_fuse_metadata.get("exact_prefix_tokens", 0)
             ),
         })
@@ -338,8 +339,21 @@ class Qwen2Model(nn.Module):
         shrinking = not torch.equal(active, target)
         transition = bool(reuse_commit)
         prefix_active = bool(metadata.get("exact_prefix_tokens", 0))
-        status = 1 if transition else (
-            2 if metadata["reuse_active"] or prefix_active else 0
+        full_prompt_positions = torch.arange(
+            metadata["org_seq_len"], dtype=torch.long,
+            device=hidden_states.device)
+        dense_full_repair = (
+            transition
+            and not prefix_active
+            and not metadata["reuse_active"]
+            and torch.equal(active, target)
+            and torch.equal(active, full_prompt_positions)
+        )
+        metadata["dense_full_repair_endpoint"] = dense_full_repair
+        status = 0 if dense_full_repair else (
+            1 if transition else (
+                2 if metadata["reuse_active"] or prefix_active else 0
+            )
         )
         start = torch.cuda.Event(enable_timing=True)
         end = torch.cuda.Event(enable_timing=True)
@@ -350,7 +364,7 @@ class Qwen2Model(nn.Module):
             self.old_kvs[int(layer) - 1])
         end.record()
         end.synchronize()
-        if transition:
+        if transition and not dense_full_repair:
             metadata["reuse_active"] = True
         return {
             "hidden_states": hidden_states, "residual": residual,
@@ -368,6 +382,7 @@ class Qwen2Model(nn.Module):
         self.cache_fuse_metadata["probekv_resumable"] = False
         self.cache_fuse_metadata["check"] = False
         self.cache_fuse_metadata["reuse_active"] = False
+        self.cache_fuse_metadata["dense_full_repair_endpoint"] = False
         self.cache_fuse_metadata["exact_prefix_tokens"] = 0
         return hidden_states
 
